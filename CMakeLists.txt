CMAKE_MINIMUM_REQUIRED(VERSION 2.8)
PROJECT(pem2c C)
FIND_PACKAGE(OpenSSL REQUIRED)
INCLUDE_DIRECTORIES(${OPENSSL_INCLUDE_DIR})
ADD_EXECUTABLE(pem2c pem2c.c)
TARGET_LINK_LIBRARIES(pem2c ${OPENSSL_LIBRARIES})
INSTALL(TARGETS pem2c DESTINATION bin)
